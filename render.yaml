services:
  - type: web
    name: voidvision-server
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
